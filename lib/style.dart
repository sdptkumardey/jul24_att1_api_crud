import 'package:flutter/material.dart';
const front_text_button = TextStyle(fontSize: 16.0,color: Colors.white, );
const front_button_padding = EdgeInsets.symmetric(vertical: 10.0);
const front_border_left = BoxDecoration(
   border: Border(
      bottom: BorderSide(color: Color(0xFF7F7F7F)),
    )
);
const front_border_right = BoxDecoration(
    border: Border(
      bottom: BorderSide(color: Color(0xFF7F7F7F)),
    )
);
const front_border_left_n_bottom = BoxDecoration(
    border: Border(
      right: BorderSide(color: Color(0xFF7F7F7F)),
    )
);
